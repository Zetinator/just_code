//https://www.hackerrank.com/challenges/self-balancing-tree/problem
/* Node is defined as :
typedef struct node
{
    int val;
    struct node* left;
    struct node* right;
    int ht;
} node; */


node * insert(node * root, int val)
{
	//execute standard insertion
	if(val > node.val){
		if node.right{
			insert(node.right);
		}
		node.right = val
	}
	else{

  
}
